import { default as matchupMessages } from './matchup'
import { default as partidaMessages } from './partida'
import { default as appMessages } from './app'
import { default as deckMessages } from './deck'
import { default as profileMessages } from './profile'
import { default as trackerMessages } from './tracker'

const messages = {
  profile: profileMessages,
  deck: deckMessages,
  matchup: matchupMessages,
  partida: partidaMessages,
  app: appMessages,
  tracker: trackerMessages
}

export default messages
